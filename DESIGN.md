# 西瓜老师·个人工作台设计规格

## 目标

让用户打开页面后三秒内知道今天最重要的事，同时保留一个不被任务挤占的书写空间。产品不是企业效率后台，而是由西瓜老师陪伴的个人桌面。

## 视觉与角色

- 基底：回声日记式暖纸白、深绿文字、克制留白。
- 个性：西瓜红、叶片绿、便签黄，粗线条与少量硬投影。
- 角色：西瓜老师使用透明背景独立角色素材，负责提醒、空状态和 AI 对话，不做夸张吉祥物。
- 字体：界面与对话统一使用本地嵌入的 OPPO Sans；大日期保留少量宋体对比。

## 信息结构

1. 今天：以对话框开场，由 AI 从自然语言提取任务；没有具体时间的任务进入收信箱。
2. 收信箱：集中保存没有具体时间的任务，可补充时间后移入时间线。
3. 年历：十二个月总览，每个月可直接填写当月要做的事，不要求具体时间。
4. 月历：月视图与按日任务，每一天直接显示最多三项。
5. 日程：顶部显示收信箱，主体保留紧凑的一行任务时间线，不出现额外引导卡。
6. 日记：先与西瓜老师进行简短、具体的事实型对话，再由用户主动生成、确认和保存日记。
7. AI 助手：由用户主动发起，整理任务。
8. 设置：DeepSeek API Key、本地数据导入导出。

## 状态与数据

- 任务、日记、聊天、设置统一保存到浏览器 localStorage。
- 删除、清空、导入覆盖均需二次确认。
- AI 生成内容只能进入草稿，必须由用户主动保存。
- API Key 仅保存在当前浏览器，不写入源码、不进入导出文件。

## 手机端

- 左侧导航折叠为底部六项导航，设置进入顶部入口。
- 卡片改单列，月历保留七列但缩短任务文字。
- 弹窗改为底部表单，输入区不被固定导航遮挡。

## 验收

- 可新建、编辑、删除、完成任务，刷新后数据仍在。
- 年历、月历、今天、日程同步显示任务。
- 日记可对话、主动生成、修改、保存、搜索和删除。
- 未配置 API Key 时有明确引导；配置后可调用 DeepSeek。
- AI 生成内容不会自动保存或覆盖日记。
- 桌面与手机无横向溢出，键盘焦点清楚，减少动态偏好有效。
